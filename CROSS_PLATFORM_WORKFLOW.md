# 🌐 跨平台无缝协作指南

## PHOTOGRAPHER 项目 - Windows 与 Mac 同步开发

---

## ✅ 工作流程总览

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub 仓库                           │
│          https://github.com/kenzhu111-git/kenspace          │
│                      (代码云端存储)                          │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ push / pull
                              ▼
┌──────────────────┐    ┌──────────────────┐
│    Windows 电脑   │    │    Mac 电脑      │
│  白天工作         │    │  晚上工作        │
│  修改 → push     │    │  pull → 修改     │
└──────────────────┘    └──────────────────┘
```

**核心原则**：
- ✅ 任何修改后立即 `git push`
- ✅ 开始工作前先 `git pull`
- ✅ 代码永远以GitHub为最新

---

## 📅 每日工作流程

### 🏠 在家（Mac）- 晚上开始工作

```bash
# 1. 进入项目目录
cd ~/Documents/kenspace

# 2. 拉取最新代码（重要！）
git pull origin main

# 3. 开始工作...
#    - 编辑文件
#    - 测试功能

# 4. 完成工作后
git add .
git commit -m "描述今晚的修改内容"
git push origin main
```

### 🏢在公司（Windows）- 白天开始工作

```powershell
# 1. 进入项目目录
cd C:\Users\kenzh\.minimax-agent-cn\projects\4

# 2. 拉取最新代码（重要！）
git pull origin main

# 3. 开始工作...
#    - 编辑文件
#    - 测试功能

# 4. 完成工作后
git add .
git commit -m "描述今天的修改内容"
git push origin main
```

---

## 🔄 同步冲突处理

如果两台电脑同时修改了同一文件，可能产生冲突：

### 冲突表现
```
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit.
```

### 解决方法

**方法1：手动解决冲突**
```bash
# 1. 查看冲突文件
git status

# 2. 打开冲突文件，会看到类似：
<<<<<<< HEAD
这是Windows的修改
=======
这是Mac的修改
>>>>>>> branch-name

# 3. 保留正确的版本，删除 <<< === >>> 标记

# 4. 解决后提交
git add .
git commit -m "解决冲突"
git push origin main
```

**方法2：放弃本地修改（谨慎使用！）**
```bash
# 放弃本地所有修改，以远程为准
git reset --hard origin/main
git pull
```

---

## 📝 提交信息规范

### 推荐格式
```
<类型>: <简短描述>

[详细说明（可选）]

[关联Issue（可选）]
```

### 类型标识
- `feat`: 新功能
- `fix`: 修复bug
- `style`: 格式调整
- `refactor`: 重构代码
- `docs`: 文档更新
- `test`: 测试相关
- `chore`: 其他杂项

### 示例
```
feat: 添加作品分类筛选功能

实现了按数码、胶片、湿版等分类筛选作品集

Fixes #12
```

---

## 💻 Mac 端设置指南

### 1. 安装 Git（如果未安装）

```bash
# 检查是否安装
git --version

# 如果未安装，使用Homebrew
brew install git
```

### 2. 配置 Git

```bash
git config --global user.name "ken"
git config --global user.email "happyyuge1518@126.com"
```

### 3. 克隆项目

```bash
# 首次设置（Mac）
cd ~/Documents
git clone https://github.com/kenzhu111-git/kenspace.git
cd kenspace
```

### 4. 启动本地服务器

```bash
# 方法1: 使用Python（Mac自带）
python -m http.server 8000

# 方法2: 使用Node.js（如果已安装）
npx serve .

# 访问: http://localhost:8000
```

---

## 💻 Windows 端设置指南

### 1. 安装 Git（如果未安装）

- 下载地址：https://git-scm.com/download/win
- 安装时**务必勾选** "Add Git to PATH"

### 2. 启动本地服务器

```powershell
# PowerShell中
.\start-dev-server.ps1

# 或手动
python -m http.server 8000

# 访问: http://localhost:8000
```

---

## 📂 常用 Git 命令速查表

| 操作 | Windows | Mac |
|------|---------|-----|
| 拉取代码 | `git pull` | `git pull` |
| 查看状态 | `git status` | `git status` |
| 添加文件 | `git add .` | `git add .` |
| 提交修改 | `git commit -m "信息"` | `git commit -m "信息"` |
| 推送到GitHub | `git push` | `git push` |
| 查看历史 | `git log --oneline` | `git log --oneline` |
| 撤销修改 | `git checkout -- .` | `git checkout -- .` |

---

## 🚨 常见问题解决

### 问题1: Git 提示需要登录

**解决方法**：
1. 打开 GitHub → Settings → Developer settings → Personal access tokens
2. 生成新的 token（选择 repo 权限）
3. 使用 token 作为密码登录

### 问题2: 冲突解决不了

**强制覆盖本地**（谨慎使用！）：
```bash
git fetch origin
git reset --hard origin/main
git pull
```

### 问题3: 文件忘记提交

**查看未提交的文件**：
```bash
git status
```

### 问题4: 想看历史修改

**查看详细日志**：
```bash
git log
```

**查看简洁日志**：
```bash
git log --oneline --graph --all
```

---

## 📋 每日检查清单

### ✅ 开始工作前
- [ ] `git pull` 拉取最新代码
- [ ] 检查是否有冲突
- [ ] 确认工作目录干净

### ✅ 工作中
- [ ] 频繁保存和提交（小步提交）
- [ ] 提交信息清晰

### ✅ 完成工作后
- [ ] `git add .` 添加所有修改
- [ ] `git commit -m "描述"`
- [ ] `git push` 推送到GitHub
- [ ] 验证推送成功

---

## 🎯 最佳实践

1. **小步提交**：每次只提交相关的修改
2. **及时推送**：每天结束前务必推送
3. **先拉后推**：开始工作前先拉取最新代码
4. **清晰描述**：提交信息要描述做了什么
5. **避免冲突**：不要多天不同步

---

## 🔗 相关资源

- **GitHub 仓库**: https://github.com/kenzhu111-git/kenspace
- **项目文档**: README.md
- **设置指南**: PROJECT_SETUP_GUIDE.md
- **SEO优化**: seo-optimization-patch.js
- **Supabase服务**: supabase-service-patch.js

---

## 📞 遇到问题？

1. 查看本指南的常见问题部分
2. 查看 GitHub 上的 Issues
3. 搜索 Google/Bing
4. 询问 AI 助手

---

**记住**：只要遵循"**修改后推送，开始前拉取**"的原则，就能实现完美的跨平台无缝协作！🎉
